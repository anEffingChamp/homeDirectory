userColor="$(colorCode $(whoami))"
hostColor="$(colorCode $(hostname))"

LP_ENABLE_SSH_COLORS=1
LP_ENABLE_TIME=1
LP_MARK_GIT=''
LP_COLOR_USER_LOGGED=$userColor
LP_COLOR_HOST=$hostColor
LP_COLOR_NOWRITE=$BOLD_RED

# We create some placeholder text for later string substitution in real time.
#LP_COLOR_PATH="{pathColor}"
#LP_PS1_PREFIX="\n{dateColor}\D{%a}"
LP_PS1_PREFIX="\n\D{%a}"
LP_PS1_POSTFIX="\n ${GREEN}>>${NO_COL} "
LP_COLOR_TIME="$colorDate"
